import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  // Dark Mode - Bright & Vibrant
  static const primary = Color(0xFF22E3FF);      // Bright Cyan
  static const secondary = Color(0xFF8A00C4);    // Bright Purple
  static const accent = Color(0xFF39FF14);       // Neon Green
  static const pink = Color(0xFFFF2CD6);         // Hot Pink

  // Light Mode - Deep & Rich
  static const primaryDark = Color(0xFF0891B2);       // Deep Cyan
  static const secondaryDark = Color(0xFF6B21A8);     // Deep Purple
  static const accentDark = Color(0xFF15803D);        // Deep Green
  static const pinkDark = Color(0xFFBE185D);          // Deep Rose

  // Dark Theme Colors
  static const darkBackground = Color(0xFF0A0E1A);
  static const darkBackgroundGradient = Color(0xFF050816);
  static const darkCard = Color(0xFF0F1520);
  static const darkBorder = Color(0xFF22E3FF);
  static const darkText = Colors.white;
  static const darkSubtext = Color(0xFFB0B0B0);

  // Light Theme Colors
  static const lightBackground = Color(0xFFFEFEFF);
  static const lightCard = Color(0xFFFEFEFF);
  static const lightBorder = Color(0xFF0891B2);
  static const lightText = Color(0xFF212121);
  static const lightSubtext = Color(0xFF757575);

  // Status Colors
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFFC107);
  static const info = Color(0xFF2196F3);
  static const error = Color(0xFFEF4444);
}
